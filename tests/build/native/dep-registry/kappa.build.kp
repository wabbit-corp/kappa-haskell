-- expect: E_DEPENDENCY_UNRESOLVED
let buildConfig : BuildConfig = package { name = "a", version = semver "1", sourceRoots = [sourceRoot "src"], fragmentAxes = [], dependencies = [registry { name = "std", version = "^1" }], hostBindings = [], targets = [executable { name = "app", backend = native { toolchain = "cc", targetTriple = "t" }, fragments = tags [], main = module "app", modules = modulesUnder "app", dependencies = ["std"], hostBindings = [] }] }
