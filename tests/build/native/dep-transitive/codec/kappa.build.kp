let buildConfig : BuildConfig = package { name = "codec", version = semver "1", sourceRoots = [sourceRoot "src"], fragmentAxes = [], dependencies = [pathDependency { name = "base", path = "../base" }], hostBindings = [], targets = [library { name = "codec", backend = native { toolchain = "cc", targetTriple = "t" }, fragments = tags [], modules = modulesUnder "codec", dependencies = ["base"] }] }