-- Source artifacts: narya docs/imports-and-scoping.rst (import
--               modifiers via Yuujinchou), narya PR #12 / PR #14 / PR #15.
-- Spec: modules.imports,
--       modules.imports.item_kind_selectors
--
-- Producer module: three public bindings consumed by the rename test.
module source_mod

public libOne : Int
let libOne = 1

public libTwo : Int
let libTwo = 2

public libDouble : Int -> Int
let libDouble n = n + n
