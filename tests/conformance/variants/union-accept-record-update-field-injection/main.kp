@PrivateByDefault module main

type Holder = (payload : (| Int | String |), tag : String)

base : Holder
let base : Holder = (tag = "n", payload = 1)

updated : Holder
let updated : Holder = base.{ payload = "s" }
