@PrivateByDefault module main


data Buf : Type =
    Buf (n : Int)

pattern Positive (& b : Buf) : Option Int =
    match b
    case Buf n -> if n > 0 then Some n else None

asExpr : Buf -> Option Int
let asExpr (b : Buf) : Option Int = Positive b
