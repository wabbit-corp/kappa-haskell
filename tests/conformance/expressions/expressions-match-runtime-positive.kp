@PrivateByDefault module main

sumList : List Int -> Int
let sumList xs =
    match xs
      case Nil -> 0
      case head :: tail -> head + sumList tail

let result = sumList (10 :: 20 :: 42 :: Nil)

--! assertNoErrors
--! assertDeclKinds signature, let, let
--! x-assertEval result 72
