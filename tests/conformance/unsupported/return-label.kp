--! assertDiagnostic error E_UNSUPPORTED

-- return@label targets a named function or labeled lambda (§18.5.1);
-- the kernel here only returns from the current do-scope, so the
-- labeled form is rejected rather than silently retargeted.
main : UIO Unit
let main = do
    inner@do
        return@main Unit
    printlnString "after"
