--! assertDiagnostic error E_UNSUPPORTED

effect Counter =
    tick : Unit -> Integer

useIt : Integer
let useIt =
    handle here (addInt 1 1) with
        case return x -> x
        case tick u k -> k 0
