@PrivateByDefault module main

data Byte : Type =
    Byte

data Vec (n : Nat) (a : Type) : Type =
    VNil

makeVec : (n : Nat) -> Vec n Byte
let makeVec (n : Nat) : Vec n Byte = VNil


type Sized : Type = (len : Nat, buf : Vec this.len Byte, tag : Nat)

resize : Sized -> Sized
let resize (s : Sized) : Sized = s.{ len = 3, buf = makeVec this.len }
