module hashcode_not_showable

import std.hash.*

trait Show (a : Type) =
    show : a -> String

badShowHashCode : HashCode -> String
let badShowHashCode h =
    show h

--! mode check
--! assertErrorCount 1
--! assertDiagnosticMatch [Ss]how|[Ii]mplicit|HashCode
