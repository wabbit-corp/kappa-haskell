module macro_failure_related_origin

makeBad : Syntax Int -> Elab (Syntax Int)
let makeBad s =
    do
        origin <- syntaxOrigin s
        failElabWith "E_TEST_MACRO_FAILURE" "macro rejected this expression" [origin]

bad : Int
let bad = $(makeBad '{ 1 + 1 })

