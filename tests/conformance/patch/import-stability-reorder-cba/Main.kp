module Main

import C.*
import B.*
import A.*

proof :
    (o : Ordering) ->
    match o
    case LT -> Unit
    case _  -> nonLT (idOrdering o) = True
let proof o =
    match o
    case LT -> ()
    case _  -> refl

answer : Int
let answer = unrelated
