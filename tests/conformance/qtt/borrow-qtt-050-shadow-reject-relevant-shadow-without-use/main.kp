module main

strictId : (>=1 x : Int) -> Int
let strictId (>=1 x : Int) : Int = x

bad : (>=1 x : Int) -> Int
let bad (>=1 x : Int) : Int =
    let >=1 x = 0
    in strictId x

--! assertErrorCount 1
