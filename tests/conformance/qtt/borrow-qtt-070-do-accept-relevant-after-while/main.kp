module main

strictId : (>=1 x : Int) -> Int
let strictId (>=1 x : Int) : Int = x

ok : (>=1 x : Int) -> Bool -> UIO Unit
let ok (>=1 x : Int) (flag : Bool) : UIO Unit =
    do
        while flag do
            pure ()
        let _ = strictId x
        pure ()

--! assertNoErrors
