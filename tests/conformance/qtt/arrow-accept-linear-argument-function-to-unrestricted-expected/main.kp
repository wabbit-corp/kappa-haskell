@PrivateByDefault module main

linearId : (1 x : Int) -> Int
let linearId (1 x : Int) : Int = x

useOmegaFn : ((x : Int) -> Int) -> Int
let useOmegaFn (fn : (x : Int) -> Int) : Int = fn 0

ok : Int
let ok : Int = useOmegaFn linearId
