module main

strictId : (>=1 x : Int) -> Int
let strictId (>=1 x : Int) : Int = x

bad : (>=1 x : Int) -> Unit
let bad (>=1 x : Int) : Unit =
    let f = \() -> strictId x
    in ()

--! assertErrorCount 1
