module main

strictId : (>=1 x : Int) -> Int
let strictId (>=1 x : Int) : Int = x

ok : (>=1 x : Int) -> Int
let ok (>=1 x : Int) : Int =
    let >=1 x = strictId x
    in strictId x

--! assertNoErrors
