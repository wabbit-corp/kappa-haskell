module main

strictId : (>=1 x : Int) -> Int
let strictId (>=1 x : Int) : Int = x

good : (>=1 x : Int) -> Int
let good (>=1 x : Int) : Int =
    let t = thunk (strictId x)
    in force t

--! assertNoErrors
