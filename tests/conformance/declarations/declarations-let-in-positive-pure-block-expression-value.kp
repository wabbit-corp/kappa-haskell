module main

-- Source issue: KT-460
-- Spec: §6.3.1
outer : Int
let outer = 1

computed : Int
let computed =
    block
        inner : Int
        let inner = 41
        outer + inner

--! assertNoErrors
--! assertType computed Int
--! assertEval computed 42
