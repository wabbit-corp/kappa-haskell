@PrivateByDefault module main

extract : Int
let extract =
    block
        let pkg : (F : Type -> Type, value : F Int) =
            (F = Option, value = Some 1)
        match pkg.value
        case Some n -> n
        case None -> 0
