module main

-- Source family: query ordering issues
-- Spec: §10.6.1
ordered : List Int
let ordered =
    [
        for x in [3, 1, 2]
        order by x
        yield x
    ]

--! assertNoErrors
--! assertType ordered List Int
