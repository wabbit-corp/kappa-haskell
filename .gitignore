dist-newstyle/
*.hi
*.o
*.dyn_hi
*.dyn_o
.ghc.environment.*

# Local native toolchain (zig); not committed
.toolchain/

# Native-backend generated artifacts
*.kappa.c
examples/native/**/build/

# Generated build lockfiles under test/example fixtures (real projects
# commit kappa.lock; these fixture locks are build artifacts)
tests/build/**/kappa.lock
examples/native/**/kappa.lock

# Per-project git-dependency cache (clones)
.kappa/

# Native executables (build outputs)
*.exe
