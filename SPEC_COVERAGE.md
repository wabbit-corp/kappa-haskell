# Spec coverage matrix

Coverage of `docs/Spec.md` (Kappa Language Specification) by this
implementation, at section granularity, for all Parts I–X plus
Appendix T. Statuses:

* **Implemented** — behaviour conforms within the noted limits and is
  exercised by tests.
* **Partial** — a meaningful, documented subset conforms; the rest is
  missing or approximated (notes say which).
* **Parsed-only** — the surface syntax is accepted by the lexer/parser
  (and usually carried in the AST), but elaboration rejects it with
  `E_UNSUPPORTED` or ignores it.
* **Not implemented** — no meaningful support.

Test references are paths under `tests/conformance/` (the in-tree
suite, 242/242 passing — `kappa test tests/conformance`) or `examples/`.
The external black-box corpus tally is in `tests/external-results.md` and
TESTING.md.

## Part I. Language Contract and Conformance

| Section | Status | Notes / tests |
| --- | --- | --- |
| §1 Design principles | Partial | Diagnostics are typed records rendered to text (§1, §3.1.8) — `src/Kappa/Diagnostic.hs`. No typed compiler artifacts/query layer (§1.2): the pipeline is a batch compiler. §1.2A (no syntax-shaped decisions post-resolution) is followed in spirit: fixity is resolved before elaboration, names by semantic identity. |
| §2 Profiles, gates, versions, conformance | Not implemented | One fixed profile; no feature-gate machinery. `unicode-names` (§2.1A) is permanently inactive; the lexer emits the gate-aware error for Unicode numeric suffixes (§6.1.6). The §3.1.3 optional source-hygiene warnings (W_UNICODE_BIDI_CONTROL, W_UNICODE_CONFUSABLE_IDENTIFIER over a small documented homoglyph skeleton, W_UNICODE_NON_NORMALIZED_SOURCE_TEXT) are always on, one per class per file (`Kappa.Pipeline.loadSourceFile`; `unicode/source-hygiene.kp`). Backend-lacking-capability rejection (§2.1) is approximated only by the test harness classifying such fixtures unsupported. |
| §3.1 Diagnostic records, codes, families | Partial | Structured records with severity, stage, code, family (`kappa.*` spec spellings or the reserved implementation prefix `kappa-hs.*`, see §3.2 row), primary span, notes, helps (`Kappa.Diagnostic`, with one shared text renderer for the CLI and the harness). Stable symbolic codes (§3.1.2). The §3.1.2A machine-readable code registry backs `kappa explain CODE` (`Kappa.Explain`): every code the compiler emits is registered — including the §22 spec-spelled `KAPPA_DERIVING_SHAPE_*` codes — (cross-checked by the cabal test suite, which scans `E_`/`W_`/`KAPPA_` literals) together with the §3.1.2A portable alias spellings (`E_TYPE_MISMATCH`, `E_APPLICATION_NON_CALLABLE`, `E_MISSING_EXPLICIT_SIGNATURE`, `E_SAFE_NAV_GENERIC_AMBIGUOUS`). A machine-readable JSON record channel (§3.1.9) is implemented (`renderDiagnosticsJson`; `check --json`/`run --json`) exposing code, family, severity, stage, primary span, labels/notes/helps, related origins, and — per §3.1.4 — the portable code alias as a `portableCode` field recoverable WITHOUT parsing prose (e.g. `code: E_APPLICATION_NONCALLABLE`, `portableCode: E_APPLICATION_NON_CALLABLE`; null when the code is already its portable spelling). Missing: sub-span labels beyond the primary span, machine fix-it payloads (§3.1.6 — notes/help only), repair ranking (§3.1.7), provenance frames (§3.1.5A), long-form explanations (§3.1.13). |
| §3.1.14/§3.1.14A Recovery | Partial | Parse-level recovery to declaration boundaries: `parser/recovery.kp` (two errors reported, checking continues). Recovery granularity is declaration-level only: a parse error *inside* a `do` block abandons the whole block, so the remaining block lines re-parse as (failing) top-level declarations and the introducer can produce a spurious `E_UNRESOLVED_NAME: 'do'` — a cascade of mislocated follow-on errors after the genuine one. The lexer stops at the first lexical error by design (documented delta; multi-error lexical fixtures in the external corpus under-count). |
| §3.2 Standard diagnostic families | Partial | The `kappa.` namespace is honored as reserved (§3.1.2/§3.1.2A): a diagnostic carries a `kappa.…` family only when it corresponds to a family the spec defines, under the spec's exact spelling (29 standardized spellings in use, e.g. `kappa.pattern.non-exhaustive`, `kappa.pattern.unreachable`, `kappa.proof.impossible-reachable`, `kappa.constructor.arity`, `kappa.borrow.escape`/`.overlap`, `kappa.quantity.unsatisfied`/`.positive-lower-bound`, `kappa.unicode.name`, `kappa.type.missing-signature`); implementation-defined diagnostics with no standardized family use the implementation-reserved prefix `kappa-hs.…` (or carry no family), which cannot collide with `kappa.`. The cabal test suite enforces the invariant against Spec.md's own text (every emitted `kappa.…` spelling must occur in the spec; everything else must be `kappa-hs.…`; every emitted family must be registered). Borderline correspondences are mapped on the catalog's "Used when" texts: e.g. record/patch/import-item unknown-name codes → `kappa.name.unresolved` (§3.2.2 lists field names, module paths and import items), prefixed-string handler failures → `kappa.macro.failure` (§3.2.12 names prefixed-string elaboration), linear drop/overuse and erased-use → `kappa.quantity.positive-lower-bound`/`kappa.quantity.unsatisfied` (§3.2.5), URL-pinning rejections → `kappa.package.reproducibility` (§3.2.15); trait-coherence, instance-shape, do-machinery, projection, seal and parse diagnostics remain implementation-defined under `kappa-hs.…` because the catalog defines no family for those situations. Families' JSON channel (§3.1.9) remains unimplemented (see §3.1 row). |
| §3.3 Path-/borrow-sensitive failure diagnostics | Not implemented | Requires borrow/quantity checking. |
| §4 Unsafe and debug facilities | Not implemented | No `unsafe`/`debug` gating, `unhide`/`clarify`, termination escapes, or audit ledger. |

## Part II. Surface Syntax and Names

| Section | Status | Notes / tests |
| --- | --- | --- |
| §5.1 Identifiers | Implemented | ASCII identifiers. §5.1A Unicode name profile: not implemented (gate inactive). |
| §5.2 Keywords | Partial | All keywords lex as identifiers and are recognized contextually. Query/handler keywords — `group`, `by`, `order`, `skip`, `take`, `distinct`, `join`, `yield`, `into`, `when`, `handle`, `deep` — are ordinary identifiers outside their clause contexts, including bare and parenthesized argument position, parameter binders, and assignment targets (`lexer/soft-keyword-identifiers.kp`): they terminate an application-argument run only inside a comprehension body (plus `by` inside a `decreases` measure), and `deep` only when immediately followed by `handle`. Residual delta: keywords that continue an enclosing construct after an expression or start a statement — `then else elif in with case except finally do where if match while for for? let let? var return break continue defer import export instance derive is as on using captures decreases` — still terminate an unparenthesized argument run context-insensitively, so a bare reference to a variable with one of those names in argument position misparses (parenthesized: parse error; bare: statement split, cascading per the §3.1.14A do-block recovery delta above). See KNOWN_SPEC_ISSUES.md #11. |
| §5.3 Comments | Implemented | Line comments; comment-only lines never affect layout. |
| §5.4 Whitespace, indentation, continuation | Implemented | Python-style INDENT/DEDENT at bracket depth zero; logical-line indentation is the first token's column. Tabs rejected: `lexer/tab-in-indent.kp`, `lexer/tab-in-source.kp`. Bad dedent: `parser/bad-dedent.kp` (`E_LAYOUT_BAD_DEDENT`). Delta: several operator-continuation lines at one constant deeper level start new statements here (single or strictly-increasing continuation lines work); §5.4's continuation text does not decide the form — see KNOWN_SPEC_ISSUES.md #13. |
| §5.5.1 Operator tokens, longest match | Implemented | Longest-match-first incl. `let?`/`for?`, `?.`, `?:`, `?name` holes, `~=`, `<[`, `]>` (see KNOWN_SPEC_ISSUES.md for a consequence). |
| §5.5.1.1 Operator sections | Implemented | Left/right/bare sections; `(infix op)`/`(prefix op)` forms parse. `(op e)` is unary prefix application when a `prefix` fixity for `op` is in scope (so `(-1)`/`(- a)` are negation per §28.2.3's `(-) : prefix 80`, with chain precedence preserved: `(- 1 + 2)` is `negate 1 + 2`); `(prefix -)` denotes `negate` (§5.5.1, §6.1.4): `fixity/paren-negation.kp`. Sections usable in check mode. |
| §5.5.2 Fixity declarations | Implemented | Block-scoped; parser emits flat chains, the resolver re-associates once fixities are known. `fixity/user-fixity.kp`; missing fixity → `E_OPERATOR_NO_FIXITY` (`fixity/no-fixity.kp`). Plain `infix` is non-associative: a same-precedence chain of non-associative operators (e.g. `1 == 2 == False` at §28.2.3's `infix 40`) is rejected with `E_OPERATOR_NON_ASSOCIATIVE` (`fixity/non-associative-chain.kp`). |
| §5.5.3 Infix gating | Partial | The §28.2.3 default fixity table is provided (`Kappa.Resolve.defaultFixities`); per-name infix-permission gating beyond fixity presence is not modeled. |
| §6.1 Numeric literals | Implemented | Radix forms (`literals/radix.kp`), fraction/exponent vs suffix split (`literals/float-exponent.kp`, `literals/suffix-term.kp`), §6.1.5 typing/defaulting: integer literals through `FromInteger` (`literals/defaulting.kp`), float literals through `fromFloat : Double -> t` when the expected type has a `FromFloat` instance, defaulting to `Double` (`literals/fromfloat.kp`). §6.1.4/§6.1.5 sign: `-` is a unary operator, never part of the literal — `-123` elaborates as `negate 123` with a nonnegative `Nat` payload to `fromInteger`, so `-123 : Nat` is rejected (`Negatable Nat` unsolved) while a user `Negatable`+`FromInteger` pair receives the nonnegative payload (`literals/negate-literal.kp`, `literals/negative-nat-rejected.kp`). §6.1.6 Unicode suffixes: gate-off error. §6.1.3: `eqDouble` is raw-bit equality; `floatEq` is IEEE equality. |
| §6.2 Boolean literals | Implemented | Prelude `Bool`/`True`/`False`. |
| §6.3.1–6.3.3 Basic/raw/multiline strings | Implemented | Escapes (`lexer/bad-escape.kp`, `lexer/unterminated-string.kp`), `#"…"#` raw forms, multiline fixed dedent (the spaces-only closing-delimiter line is a content line, so literals keep their trailing newline: `lexical/multiline-trailing-newline.kp`). Source files that are not valid UTF-8 are an `E_UNICODE_INVALID_UTF8` error with U+FFFD recovery (`unicode/invalid-utf8.kp`). |
| §6.3.4 Prefixed strings + interpolation | Partial | The `f` handler with `${expr}`/`$name` interpolation works end to end (`run/string-interp.kp`, `examples/todo.kp`). Every other prefix resolves by ordinary term name resolution to an `Elab (InterpolatedMacro t)` handler whose `buildInterpolated` runs over the §6.3.4.4 fragment list at elaboration time (`macros/interpolated_macro.kp`); raw `#{…}` interpolation openers, `${expr : fmt}` format specifiers (split at the top-level `:`), and nested string literals inside interpolations are lexed per §6.3.4.1–2. Bare object-phase `InterpolatedMacro` values are rejected as handlers (`E_PREFIX_RUNTIME_HANDLER`); meta-phase bare values (the §6.3.4.3 `pure p` reading) are conservatively rejected too. |
| §6.3.5 Conventional type-prefix handler | Implemented | `type"…"` lexes as a prefixed string (the §5.2 soft keyword `type` followed adjacently by a string), and the implementation provides the conventional built-in type-producing handler: the literal content is parsed as a type expression and elaborated, yielding that `Type` (`macros/type-prefix-handler.kp` — `type"List Int"`, `type"Integer"` in annotation position). `type` cannot be an ordinary binding, so this handler is built in rather than prelude-defined. Interpolation (`type"…${e}…"`) is rejected (`E_PREFIX_HANDLER_TYPE`): splicing a runtime value into a static type is not a meaningful type-producing form. |
| §6.4 Unicode scalar literals | Implemented | `LitScalar`; `eqScalar`/`showScalar` primitives; `Char = UnicodeScalar` alias (§28.2 compatibility alias). |
| §6.5 Prefixed quoted literals | Partial | Plain scalar literals plus the conventional `g`/`b` handlers built in (not user-extensible `QuotedLiteralMacro`s): `g` requires a text view with exactly one extended grapheme cluster (UAX #29 over the embedded UCD 15.0.0 tables, `Kappa.Unicode`), `b` requires a one-byte view (`\u` escapes contribute a byte only when the scalar is ≤ U+007F); rejections are `E_UNICODE_INVALID_GRAPHEME_LITERAL`/`E_UNICODE_INVALID_BYTE_LITERAL` (§3.1.3), other prefixes stay `E_UNSUPPORTED` (`unicode/quoted-literals.kp`, `unicode/quoted-literals-reject.kp`). |
| §6.6 Unit and tuples | Implemented | `Unit`; tuples as positional records, tuple patterns (`match/tuple-record-patterns.kp`), punned tuples. |
| §7.1 Ordinary lexical lookup | Implemented | Scope-stack lookup during elaboration; `names/unresolved.kp`, `names/duplicate.kp`. §7.1.1 kind-qualified name expressions: not implemented. |
| §7.2 Same-spelling families, type facet | Partial | Type positions prefer the type facet of a head name (`Check.hs` "type-facet lookup"), recursively: any application checked against a universe re-enters the type-facet path, so nested parenthesized type arguments like `Wrap (Wrap Integer)`, `List (Wrap Integer)`, `Pair (Wrap Integer) Bool` elaborate correctly (`types/nested-same-spelling.kp`); a module-qualified name in type position likewise selects the type facet of a same-spelling family (`main.Cell` in an `assertType`). Residual: a same-spelling name in a *non-sort* type argument position (e.g. an argument checked against `Type -> Type`) still resolves in the term facet; unreachable in practice since higher-kinded data parameters are themselves unsupported. The full data-family/static-member story is absent. |
| §7.3 Dotted name resolution | Partial | Projection, method-style member access, safe navigation; module-qualified dotted paths only within the loaded-suite module graph. |
| §7.4 Method-call / receiver sugar | Partial | Method-call sugar uses the receiver-marked binder: receiver positions are recorded per global; eligibility requires exactly one receiver-marked explicit binder (none or two → ill-formed member step); the receiver inserts at that position, with preceding explicit binders becoming wrapper-lambda parameters filled by call-site arguments. Usage analysis accounts the receiver as an ordinary argument at the receiver binder's position, so linear receivers are consumed. §16.4.1 branch-local rigid facts feed conversion (§31.1.2; see §31.1 row). Receiver-projection sugar: a selector-form projection with exactly one receiver-marked `place` binder takes method-call sugar, with trailing call-site arguments filling the remaining binders in declaration order (§7.4/§9.1.1; `projections/receiver-sugar-args.kp`); a same-named record field of the receiver keeps §7.3 precedence. |
| §7.5–7.6 Module values, reified static-object facets | Partial | Reified module objects carry identity tags and survive rebinding (`let M = module Api` … `M.answer`), with member access through rebindings and records; kind-qualified static-object expressions (`(module a).b`); data-facet preference with constructor fallback for applied type heads; manifest-binding gating — a binding whose ascribed (widening) signature is not manifestly the static object forgets static identity for downstream dotted lookup (§7.6 "otherwise well-formed" rule). §30.2.2.12 identity preservation threads through seals: a signature-governed `let` keeps static-object identity when its definiens is an identity-preserving form (rebinding/sealing/opening/projection preserve semantic-object identity), and manifest projection unwraps transparent seal layers. Not: projection-declaration facets, effect-label facets, trait facets in constraint position (`let C = trait Sized`). |
| §8 Modules, imports, exports, visibility, opacity | Partial | Multi-file directory suites compile in import order with cycle detection (§8.2; `Kappa.Pipeline`). In package mode an explicit module header that disagrees with the §8.1 path-derived name is `E_MODULE_PATH_MISMATCH` (suppressed for files that already failed §3.1.3 UTF-8 decoding); the harness's `scriptMode` compiles without path-derived names. Import statements select scope: wildcards with `except` lists, items with §8.3.1 kind selectors (`term`/`type`/`trait`/`ctor`), aliases, singletons. §8.5 default-export surface enforced at imports: every ordinary top-level named item is exported unless `private` (`@PrivateByDefault` flips the default; non-exported item → `E_IMPORT_ITEM_NOT_FOUND`). Visibility is a property of the named *item* (the §8.5.1 binding group), not of an individual declaration: a `private` marker on EITHER the signature or the defining `let` of a binding group makes the whole item private, so the split `secret : Int` / `private let secret = …` (and its mirror) is correctly not exported (`modules-private-visibility/`). Default exports **include trait member projections** — a trait declaration binds each member name at module scope (§14.2.1), so members ride wildcards and `term` items (`modules-trait-members/`); §8.4 aliased re-exports preserve origin identity (`modules/facade.kp`). Instances participate cross-module per §14.3 (not via the export surface, §8.5). Not implemented: opacity beyond §8.5.3 `opaque data`, URL imports (rejected with the §8.3.2 pinning codes `E_URL_IMPORT_*`), import hashes, `unhide`/`clarify`. |

## Part III. Declarations and Static Language

| Section | Status | Notes / tests |
| --- | --- | --- |
| §9 Declarations and definitions | Partial | Signature + `let` pairs, `data`, `type` aliases, `trait`, `instance`, `fixity`, `import`; two passes (headers then bodies) per the preceding-signature recursion rule (§9.2, §15.16); a signature may precede its definition at a distance, including signatures whose types reference signature-less `let`s (deferred elaboration). Multi-line signatures continue across lines after a trailing arrow or `forall`-dot (§5.4 continuation). `pattern` (active patterns, §17.3) and `projection` declarations (§9.1.1: get/set/sink accessor bundles, synthesized `open`, capability checking — `projections/*.kp`) are implemented. `derive`, top-level splices: Parsed-only → `E_UNSUPPORTED` (`tests/conformance/shape/decl-kinds.kp` counts declaration kinds). |
| §10 ADTs and type aliases | Implemented | Parameterized `data` with named constructor fields, constructor-arity checking (`application/ctor-arity.kp`), named constructor application with field defaults (§10.1.1: a missing label is permitted iff the parameter has a default, which is elaborated at the application site — `records/ctor-field-defaults.kp`, `records/ctor-field-default-missing.kp`; defaults referring to earlier fields of the same constructor are not supported), transparent aliases (`Int`, `UIO`, …). Same-spelling constructor/type families work in nested type positions (`types/nested-same-spelling.kp`; see §7.2 row for the residual non-sort-position limitation). Data parameters must have kind `Type`: a higher-kinded parameter such as `(f : Type -> Type)` is accepted syntactically but its application in a field type fails (`E_APPLICATION_NON_CALLABLE`). GADT-style indexed results: "GADT-lite" — the constructor result is unified with the scrutinee type at match (§17.1.2 approximation). |
| §11.1 Universes | Partial | `Type`, `Type0..n`, `*` spellings; cumulativity `Type m ≤ Type n` for `m ≤ n` (§11.1.1). No universe polymorphism; records/Pi types currently land in `Type 0`. |
| §11.2 Classifiers | Not implemented | No `Row`/`Label`/intrinsic classifier names. |
| §11.3 Implicit binders, universalization | Partial | §11.3.3 approximated: free ASCII-lowercase heads in top-level signatures, instance heads, and instance premises that resolve to no global are implicitly universalized as erased implicit binders (`application/lambda-hof.kp` uses `apply2 : (b -> a -> b) -> b -> a -> b`). Binder kinds are inferred from use (a fresh kind meta defaulted to `Type`), so e.g. `BridgePackage sig` binds `sig` at kind `Region -> Type`. At definitions checked against a signature, an *annotated* implicit definition binder claims the first implicit Pi whose domain matches its annotation, leaving synthesized binders auto-bound (`let f (@d : Trait a) = …` against `forall a. (@_ : Trait a) -> …`). Block-local signatures are not universalized. |
| §11.4 Propositions | Partial | §11.4.1 propositional `(=)` with `refl` (`equality/refl-conversion.kp`); `(lhs = rhs)` implicit goals decided by conversion (`1 + 1 = 2` by refl; `5 - 3` via `subDefined` proof: `equality/sub-proof.kp`; symbolic `x - y` fails `E_IMPLICIT_UNSOLVED`: `equality/sub-unknown.kp`). §11.4.2 boolean-to-type coercion: not implemented. |
| §12 Functions, binders, quantities | Partial | All five portable interval quantities (`0`, `1`, `ω`, `≤1`, `≥1`) parse and are carried on core Pi binders, where they participate in type identity (§31.1) with demand-interval subsumption along the outer Pi spine (§12.2.1, no deep subsumption). `Kappa.Usage` enforces usage counting per definition: sequential addition / branch joins / loop zero-runs (§12.2.2), positive lower bounds on every completion path incl. return/break/continue exits (§12.2.5, §18.2), argument occurrences counted at the callee's demand interval (§12.2.2), latent (per-call) capture usage for lambdas/thunks/`lazy`/sections multiplied by consumer demand (§16.2.1), and erased-use checks. Emits `E_QTT_LINEAR_DROP`, `E_QTT_LINEAR_OVERUSE`, `E_QTT_ERASED_RUNTIME_USE`. Symbolic quantities approximate to `ω`. |
| §12 Borrowing, regions, captures | Partial | `&`, `&[region]` parse; `T captures (r..)` elaborates to a canonical `__captures` spine (sorted region order) that participates in type identity — capture subsumption is *not* definitional equality, closure literals introduce at the underlying type, eliminations peel the annotation, and capture-annotated record fields check field-directed (§12.3.1/§31.1; `qtt/captures-identity.kp`). `Kappa.Usage` enforces: borrowed bindings cannot be consumed (`E_QTT_BORROW_CONSUME`), closures/thunks capturing anonymous borrows cannot escape through results or unrestricted parameters (`E_QTT_BORROW_ESCAPE`), `let &` path borrows lock overlapping record paths for their lexical scope and same-call borrow/consume arguments must be disjoint (`E_QTT_BORROW_OVERLAP`, §12.4), quantity-1 field projection consumes the path with patch-restore (§12.4), borrowing or re-projecting a path (or whole binder) after an overlapping definite consume without an intervening restore is rejected (`E_QTT_PATH_CONSUMED`, kappa.path.consumed §3.2.6; `qtt/borrow-after-consume.kp`, `qtt/borrow-before-consume.kp`), `using` resources cannot escape (§19.5), `inout`/`~` markers are matched and threading checked (§18.9.3). No region inference; `BorrowView`/`captureBorrow`/projection declarations (§12.4.3) unsupported. |
| §13.1–13.2 Records | Implemented | Closed records: literals, punning (`records/punning.kp`), projection (`records/basics.kp`, missing field → `E_RECORD_PROJECTION_MISSING_FIELD`), functional patch `.{ }` (§13.2.5; unknown field → `E_UNKNOWN_FIELD`, `records/patch-unknown-field.kp`), rest patterns. Canonical lexicographic field order with source-order initializer evaluation (§31.4). Nested patch paths and projection-section updates `r.{ (.proj) = v }` (§13.2.5, §30.2.2.4). Dependent telescopes with `this` sibling references (§13.2.1): dependency-ordered elaboration, cycle detection, dependent projection/literal/update with stale-field repair checking (`records/dependent-*.kp`); a bare identifier in sibling-reference position is shorthand for `this.label` when unambiguous and joins the field-dependency graph. Suspension-marked fields `(thunk x : T)`/`(lazy x : T)` declare `Thunk`/`Need` fields; record literals and updates check field-directed so §16.1.7.1 suspension insertion fires, including nested records. Open rows `(fs | r)` over `RecRow` with `LacksRec` constraints and `:=` row extension (§11.3.1A, §13.2.6, `records/open-rows*.kp`); row-polymorphic functions apply to closed records — unification splits the closed fields into the explicit prefix and a `__closedRow` residual tail, `LacksRec` is solved by the §11.3.1A compiler-owned introduction rule (solvable iff the closed tail lacks the label; flex-row goals postpone to the pending queue), and `__rowExtend` computes on record values at runtime (`records/open-rows-closed-call.kp`, `records/open-rows-lacks-closed.kp`). Patch punning rejected (`E_RECORD_PATCH_INVALID_ITEM`). |
| §13.1 Variants / closed unions | Implemented | `(\| A \| B \|)` types, expected-type-directed injection incl. literals (`variants/injection.kp`), widening as a no-op, closed-union match (`variants/closed-union.kp`, missing member → error: `variants/missing-member.kp`), `Int?` Option sugar including parenthesized heads — postfix `?` adjacency is measured against the last consumed token, so `(Unit -> Int)?` and `(Int, String)?` parse (§13.1.9; `variants/option-sugar.kp`). Variant-typed record fields get expected-type-directed injection; a standalone `(\| e \|)` with no expected union type reports the term-grammar rejection. Member identity = canonical rendered member type after alias normalization (§31.3). Not: open variant rows. |
| §13.2.10 Sealed packages, opaque members | Partial | `seal e as S` with signature matching in dependency order under `this`-substitution; hidden extra fields elaborate then discard; signature types carry their opaque label set (core `CSealE`/`CSigT`, runtime `VSealV`/`VSigT`). Opaque member projections stick during conversion while transparent members and runtime projections compute through the seal; seal is pure/non-generative (`seal e as S = e`); mismatches headed by an opaque seal projection report `E_SEAL_OPAQUE_UNFOLDING`. Rejections: open records (`E_SEAL_OPEN_RECORD_ASCRIPTION`), direct literals at signature types (`E_SEAL_DIRECT_LITERAL_FOR_SIGNATURE`). `records/seal-opaque.kp`, `records/seal-errors.kp`. |
| §13.2.11 Anonymous existentials | Implemented | `exists (a1:S1)...(an:Sn). T` elaborates as surface sugar to the §13.2.10 sealed-package machinery (`elabExists`): the witness binders become positional internal members `⟨wit_i⟩` (each `opaque 0`, erased static metadata §31.2) that source code cannot spell, so they are never projectable (`types/exists-witness-not-addressable.kp` pins `E_RECORD_PROJECTION_MISSING_FIELD`); a record/signature payload contributes its fields under their source labels, any other payload becomes one anonymous `⟨payload⟩` member. Internal labels are position-based so alpha-equivalent existentials share a shape (Spec.md 13322). `seal e as exists ...` (`ESealExists`) packs a value and `open` (`EOpenExists`) unpacks it, threading the §12.4.3 borrow-escape check (`types/exists-existential.kp`). |
| §14 Traits | Partial | User traits, instances, premise instances (`instance Eq a => Eq (List a)`: `traits/premise-instance.kp`), default members (`traits/default-member.kp`). Resolution: local implicit context, then global instance search with a unique-candidate coherence rule (§14.3.1) — ambiguity → `E_INSTANCE_INCOHERENT` (`traits/incoherent.kp`), no candidate → `E_IMPLICIT_UNSOLVED` (`traits/missing-instance.kp`). Program-level coherence (§14.3/§33.2.1): overlapping instances are rejected at the (later) declaration even when unused, unless the instantiated dictionary artifacts are definitionally equivalent (structural coherence mode; equivalent duplicates are harmless overlap with a deterministic representative at use sites), with candidates whose premises are unsolvable at the shared instantiation discarded before coherence per §33.2.1 step 1 (`traits/overlap-incoherent-unused.kp`, `traits/overlap-equivalent.kp`). Instance visibility within a module is declaration-order-insensitive (§14.3: visibility is global over the module closure): instance heads are registered in a pre-pass ahead of all declaration bodies, so a use site that precedes the instance declaration still resolves through it (`traits/instance-after-use.kp`), and a use site between the members of an overlapping pair does not pre-judge coherence while a dictionary is still being defined — the later declaration judges the pair (`traits/incoherent-after-use.kp`). Supertrait premises (§14.1.4) are enforced at instance declarations — a tupled premise list `trait (C1 a, C2 a) => T a` splits into individual supertrait premises — missing evidence → `E_SUPERTRAIT_UNSATISFIED` (`traits/supertrait-unsatisfied.kp`); satisfiable from global instances or the instance's own premises, including depth-bounded transitive supertrait conformance paths (`traits/supertrait-satisfied.kp`). Higher-kinded traits: instance heads whose arguments are type *constructors* (`instance Functor List`) check the head arguments against the trait-constructor parameter types, backing the §28.2 container trait stack (`prelude/containers.kp`); kind-like implicit goals (`Type -> Type`) are never evidence-searched, and unification decomposes first-order meta spines and rigid spines (`?f a ~ G a`). Not: supertrait conformance paths during *use-site* implicit resolution (evidence of `MyOrd a` does not yield `MyEq a` at member call sites), associated type members (`Out : Type`, §14.2.1), associated static members (§14.3.4), named instances, instance search-depth control (§14.3.5 beyond a recursion bound). |
| §15 Totality, termination, unfolding | Partial | §15.1 split between total-certified and conversion-reducible is modeled: only reducible definitions δ-unfold during conversion. Structural-descent verification for direct recursion (§15.3 minimum): `recursion/structural.kp` (clean), `recursion/non-structural.kp` (`W_TERMINATION_UNVERIFIED`), recursion without a preceding signature → `E_RECURSION_NO_SIGNATURE` (`recursion/no-signature.kp`). `decreases` clauses parse in all portable forms — bare measure, parenthesized tuple, `by` relation, and `structural x` (`recursion/decreases-parses.kp`) — and are recorded but not verified against §15.4/§15.8; §15.10–15.17 lanes not implemented. |

## Part IV. Expressions, Control Flow, and Effects

| Section | Status | Notes / tests |
| --- | --- | --- |
| §16.1 Application, spine, implicits | Implemented | Application-spine implicit insertion (§16.1.7.1) with postponed trait goals; named function application and named-argument blocks (§16.1.7) — a malformed named-argument block on an ordinary callee is one telescope mismatch, while a malformed *constructor* block (missing/duplicate/unknown field) is a malformed constructor application under family `kappa.constructor.arity` exposing the §3.2 portable alias `E_CONSTRUCTOR_ARITY_MISMATCH` (rendered as the specific `E_NAMED_ARG_*` code, recovered via the §3.1.4 alias table), and the block must be the final argument (§16.1.7.2); non-callable application → `E_APPLICATION_NON_CALLABLE` (`application/non-callable.kp`). |
| §16.1.1 Dotted forms | Partial | Projection, method sugar (receiver-marked binders, §7.4 row), safe navigation `?.` (§16.1.1.2). Projection-declaration selectors and accessor bundles (§9.1.1, §16.1.6): get/set/sink clauses, synthesized `open`, capability-required rejection (`E_PROJECTION_CAPABILITY_REQUIRED`), footprint/zipper checks (`projections/*.kp`). General first-class projector/accessor *descriptor* values beyond declared projections: not implemented. |
| §16.1.2 Elvis `?:` | Partial | `a ?: b` elaborates as Option scrutiny when the left operand is Option-typed (a dedicated `EElvis` node: `Some x ?: b ↦ x`, `None ?: b ↦ b`); general null-union / flow-typed left operands beyond `Option` follow the variant story and are not special-cased. |
| §16.1.3 Short-circuit booleans | Implemented | `(&&)`/`(||)` over `Thunk Bool` with suspension binder sugar (`(thunk x : T)` ⇒ `Thunk T`, §11.2) and `force (thunk e) ↦ e` reduction. |
| §16.1.4 / §16.1.8 Subsumption, equality transport | Partial | Variant widening and alias-transparent conversion at application boundaries; automatic transport insertion limited to conversion-decided `(=)` goals. A bare rigid `Bool` condition in `if` registers the §16.4.1 branch-local fact (`b = True`/`b = False`), so indexed types convert under the refinement in each branch (§31.1.2). At application boundaries a same-head indexed-type mismatch reports `E_APPLICATION_ARGUMENT_MISMATCH` (a transport with no licensing evidence) instead of a bare equality mismatch. |
| §16.2 Lambdas | Implemented | Typed/untyped binders (including multi-argument unannotated lambdas against polymorphic HOF parameters: `application/lambda-hof.kp`), implicit binders, suspension sugar. |
| §16.3 Implicits, holes | Partial | §16.3.3 ladder: local implicit context → global instance search → boolean-proposition normalization for `(lhs = rhs)` goals decided by conversion. Local-candidate rules (`implicits/local_candidates.kp`): context entries carry the binder's quantity and borrow marker — erased candidates are rejected for runtime goals (`E_QTT_ERASED_RUNTIME_USE`), borrowed candidates do not cross closure barriers, two same-scope candidates are ambiguous (`E_IMPLICIT_AMBIGUOUS`), name shadowing hides outer same-name candidates (anonymous binders never shadow), and a runtime implicit binder with no candidate reports `E_IMPLICIT_UNSOLVED` rather than leaving a silent meta; block-local `let (@q x : T) = e` bindings join the local implicit context (`do/implicit-do-binding.kp`). Flex goal *types* are inferred by unification, never evidence-searched. Named holes lex; no interactive hole reporting. |
| §16.4 Conditionals, flow typing | Partial | `if`/`elif`/`else` expressions (else required in value position: `run/if-expr-needs-else.kp`); statement `if` without `else` in do-blocks (`run/if-no-else.kp`). Flow-sensitive refinement: `if p is C` constructor tests refine the scrutinee's constructor set in the branch (§16.4.1), composed through `&&`/`\|\|` (§16.4.2) and transported across stable aliases `let q = p` (§16.4.3), enabling field projection on the refined multi-constructor subset (`refinement/is-projection.kp`, `refinement/is-disjunction-alias.kp`). A bare `x is C` test also refines `x` in the *negative* branch when exactly one residual constructor remains (a wider residual invents no positive fact). Not: `not`-composition, `match`-arm refinement outside `if`, §16.4.4 positive lower-bound checking. |
| §17 Patterns, matches | Partial | Constructor, literal, tuple, record, variant, or-patterns (`match/or-patterns.kp`), guards (`match/guards.kp`), nesting (`match/nested-ctor.kp`), parenthesized-operator constructor heads (`((::) x xs)`). Exhaustiveness (§17.1) for closed ADTs, `Bool`, variants, records, tuples; literal scrutinees require a catch-all (`match/literal-patterns.kp`, `match/not-exhaustive.kp`, `match/bool-missing-case.kp`). Active patterns (§17.3): implemented — `pattern` declarations (Option / `Match item residue` / total-view results; monadic results and linear-scrutinee Option patterns rejected), match-case lowering with residue threading, `let?` rewriting, pattern-as-value (`patterns/active_patterns.kp`). `impossible` branches, erased-scrutinee discrimination, guard evidence: not implemented. |
| §18.1 Effects, fibers | Partial | Algebraic effects over §9.3.1.1 `scoped effect` declarations: `Eff : EffRow -> Type -> Type` with closed effect-row syntax `<[l : E]>` (rows are neutral `__effRowCons` spines; no row polymorphism/weakening, multi-entry rows keep source order), operation invocation `label.op arg` (multiple explicit parameters per operation per §18.1.21, `op : Π(x₁:A₁)…(xₙ:Aₙ). B`, selected as the curried value `A₁ -> … -> Aₙ -> Eff <[..]> B` and handled by `case op x₁ … xₙ k` — `effects/effect-multiarg.kp`), `handle`/`deep handle` with `case return`/operation clauses typed per §18.1.21–.22 (target carrier fixed to `Eff residual` in v1), `runPure` (§18.1.14), and one-shot-by-default resumption quantities (§18.1.16–.17). Runtime: an `Eff` value is a `__EffPure`/`__EffOp` tree (the §30.2.2.7 OpCall kernel) reduced by `Kappa.Eval.evalEffPrim`; continuations are ordinary closures, so multi-shot resumption is re-entrant per §32.2.14 (`effects/*.kp`). Static discipline in `Kappa.Usage`: the clause binder `k` is counted at the declared resumption quantity (declared `1` counts as at-most-once because a clause may abandon its segment, §32.2.20), `>=1` unused → `E_QTT_LINEAR_DROP`, overuse → `E_QTT_LINEAR_OVERUSE`, and the §18.1.20 call-site capture rule rejects a multi-shot operation whose lexical do-suffix uses a quantity-1 or borrowed binding (`E_QTT_CONTINUATION_CAPTURE`). `Eff`-typed do-blocks elaborate to `__effBind` chains (bind/let/expression/final-if items only). Top-level (module-scope) `effect` declarations (§18.1) are also implemented: a top-level `effect E = …` mints a module-wide interface + label registered in `csModEffLabels`, visible to every definition (effect resolution consults it as a fallback to the lexically-scoped `ctxEffLabels`); operations are invoked `E.op arg`, the label appears in rows `<[E : E]>` and is handled by `handle E`/`deep handle E` exactly like a scoped effect (`effects/top-level-effect.kp`, result 20). The §18.1.4–18.1.8 fiber/concurrency model is implemented in the interpreter on a weak-fair cooperative single-agent scheduler (the threaded RTS pinned to one capability, §18.1.5 cooperative realization): `fork`/`forkDaemon`/`await`/`join`/`interrupt`/`interruptFork`/`interruptAs`/`interruptForkAs` with the structured §18.1.2 `InterruptCause`/`InterruptTag` vocabulary and `Exit`/`Cause` outcomes; `cede` and a fairness guarantee where a parked fiber is resumed by another fiber's progress; monotonic timers (`nowMonotonic`/`sleepFor`/`sleepUntil`, interruptible, parking only the current fiber) and `timeout`/`race`; §18.1.13 STM (`newTVar`/`readTVar`/`writeTVar`/`retry`/`check`/`atomically`) where `retry` suspends until a read TVar changes; §18.11 one-shot promises (`newPromise`/`awaitPromise`/`completePromise`) that suspend until completion; §18.1.7 fiber-local state (`newFiberRef`/`getFiberRef`/`setFiberRef`/`locallyFiberRef`) with snapshot-on-fork inheritance; §18.1.8 explicit supervision scopes + monitors (`newScope`/`withScope`/`forkIn`/`shutdownScope`/`monitor`/`awaitMonitor`/`demonitor`); and §18.1.2 `sandbox`/`unsandbox` (`effects/{scheduler-retry,timeout,race,interrupt,scope,monitor,fiber-local,fiber-fork-await,stm-tvar,promise,timer-duration}.kp`). The §18.1.15 `effect label l : E` form is implemented both top-level and scoped: it mints a fresh effect label sharing an existing effect's interface and operations but with its own label identity, usable in `l.op`, rows `<[l : E]>`, and `handle l`/`deep handle l` (`effects/effect-label-toplevel.kp`, `effects/scoped-effect-label.kp`, `effects/effect-label-decl.kp`). Parameterized effect interfaces (`effect State (s : Type) = …`, including multiple parameters) are implemented: the interface becomes a type constructor `State : k0 -> … -> Type`, operation signatures elaborate under the parameter binders, the applied interface appears in rows (`<[State : State Int]>`), and the type parameters are threaded through operation selection (`l.op : forall params. arg -> Eff <[l : E params]> res`) and handler clause typing (the matched row interface fixes the parameters) — verified in both backends (`effects/effect-parameterized.kp`, `effects/effect-parameterized-two.kp`). A handler may eliminate into a non-`Eff` target carrier such as `IO e` (§18.1.21; a deep handler runs its clause bodies and resumption in that carrier — `effects/handler-io-carrier.kp`), and operations may carry their own forall/implicit parameters (§18.1.15; `effects/effect-forall-op.kp`). Not implemented: row variables/`SplitEff` polymorphism, completion-carrying handlers (§18.1.21.1/.22.1); and native-backend concurrency (the C backend keeps the single-agent direct model). The native C backend now ALSO runs algebraic effects: `__EffPure`/`__EffOp` are fixed-tag builtin constructors, the OpCall-kernel primitives `runPure`/`__effBind`/`__handleEff` are implemented in `kappart.c` (mirroring `Kappa.Eval.evalEffPrim` — constructor match, `kclo` continuation closures, deep-handler reinstall, `kproj` op-record dispatch), and effect labels lower to string-token identities (native-only `recordCoreBody`; the interpreter keeps neutral-global identity). Native output is verified identical to the interpreter for deep/multi-shot/shallow/top-level effects (`test/native/cases/effects.kp` → `20|21`; the scoped `effects/multishot-resumption.kp` and `shallow-abandon.kp` patterns build native → 21/42). |
| §18.2–18.8 do-blocks, kernel | Partial | The §18.8 completion kernel is implemented structurally (`CDo` in core, executed by `Kappa.Interp`): completion records, LIFO `defer` (`run/defer-lifo.kp`), `return` (`run/return-early.kp`), `break`/`continue`, loop `else` only on no-break (`run/loop-else-break.kp`, `run/for-break-continue.kp`), `while` (`run/while-var.kp`), `for`, `let pat <- e` monadic bind, `let? … else` (`run/letq-else.kp`), `var` cells as `Ref` with auto-deref reads (§18.6.1), `!e` splices (§18.3). Labeled loops with `break@L`/`continue@L` (§18.2.5) including compile-time label resolution (`E_LABEL_UNRESOLVED`; `labels/*.kp`); unlabeled `break`/`continue` outside any loop body of the do-scope → `E_BREAK_OUTSIDE_LOOP` (§18.6; `do/continue-outside-loop.kp`). A nested `do` block in STATEMENT position (an explicit `then do …`, or an `L@do …` statement) is TRANSPARENT to abrupt completion per §18.8.3.1: it runs as a child scope (its own label, bindings, and LIFO exit actions) but `break`/`continue`/`return@L` written inside it resolve against the inherited lexical context and propagate to the enclosing loop/function, while its own `defer` runs at the nested exit and `defer@L` reaches the labeled enclosing scope — verified in BOTH backends (`do/nested-do-completion.kp`, `labels/return-label-cross-do.kp`; the elaborator emits a `KSubDo` item, the interpreter runs it as a completion-propagating child scope, the native backend lowers it as a Nested defer scope). A first-class do VALUE (`let inner = do …`, or a spliced `!inner`) is by contrast an opaque computation with no lexical completion target at its definition site, so `break`/`continue`/`return@L` inside it are rejected at compile time (`do/break-in-first-class-do.kp`, `labels/return-label-in-do-value.kp`) — this matches §18.6 (targets must syntactically occur within an enclosing loop of the same function body). The only carrier is `IO e` — do-blocks are not generic over user monads. `defer@label` (§18.7) is implemented: a `defer@L` schedules onto the enclosing labeled do-scope L (an explicit `L@do` or a loop `L@for`/`L@while`), so it stays pending across the exits of inner scopes (loop bodies, `if`-branches) and runs LIFO only when L itself exits — verified in BOTH the interpreter and the native C backend (`labels/defer-label-self.kp`, `labels/defer-label-outer.kp`; native routes via a label→defer-frame map with subtree-aware frame sizing). An explicit `do` label and loop label are now consumed by `defer@L`; a label naming no enclosing labeled do-scope is `E_LABEL_UNRESOLVED`. `return@label` (§18.5/§18.5.1) is implemented: `return@L` exits the enclosing named function/method (`return-label-function.kp`), the §9.1 inherited-name lambda (`let f = \x -> …`), or a labeled lambda `L@\ x -> …` (`return-label-lambda.kp`), transparent through enclosing `if`/loops; resolution is confined to the current function/lambda body (`ctxReturnTarget`) and a label naming none is `E_LABEL_UNRESOLVED`. Verified in both the interpreter and the native backend. A `return@L`/`break`/`continue` is rejected only when its target is reachable solely across a first-class do-VALUE boundary (`return-label-in-do-value.kp`), which is the spec-correct behavior (§18.6). |
| §19 Errors, try/except/finally | Partial | Typed IO errors: `raise`/`throwError`/`catchError`, `try`/`except`/`finally` statements (`run/try-except-finally.kp`, `examples/todo.kp`). `try match` and `bracket`-style resources: not implemented. |
| §20 Collections, ranges, comprehensions | Partial | Comprehension pipeline lowered over lists per the §20.10.11 as-if rule: `for`/`for?`/`let`/`let?`/`if`, stable `order by` (asc/desc, multi-key), `skip`/`take` with §20.3.2 orderedness tracking (`E_QUERY_UNORDERED_PAGING`), `distinct`/`distinct by`, `group by { aggs [using W] } into`, `join`, `left join … into`; map comprehensions with `on conflict` policies; set/map literals; carrier prefixes `Query`/`OnceQuery`/`SingletonQuery`/`QueryCore mode q`/`Array` with §20.10.1 mode/cardinality/item-quantity plan checks and §20.10.4 row-quantity diagnostics (`queries/clauses_runtime.kp`, `queries/carriers_qtt.kp`). Ranges `..`/`..<`: fixity exists, no prelude implementation. Custom sinks: `FromComprehensionRaw`/`FromComprehensionPlan` instances run their `Elab` hook at elaboration time (raw preferred, §20.9), with the associated `Item` checked definitionally equal to the yielded item type (`E_SINK_ITEM_MISMATCH`) and `Query {…}`/`Query {\|…\|}` map/set metadata loss rejected (`E_QUERY_METADATA_LOSS`); the hook receives an opaque carrier token, not an inspectable `RawComprehension`/`ComprehensionPlan` value (`collections/custom_sink.kp`). |

## Part V. Metaprogramming and Staging

| Section | Status | Notes |
| --- | --- | --- |
| §21 Syntax, macros, elab, reflection | Partial | `Syntax t` quotes `'{ … }` with `${…}` in-quote splices (§21.1), expression/type splices `$( … )` (§21.2), macros as ordinary functions (§21.3), §21.4 hygiene via per-quote capture metadata (fresh hygienic spellings + binding levels; splice validates every capture against the splice-site context, covering local-binder and borrow-scope escape — `E_SYNTAX_SCOPE_ESCAPE`), and §21.9 `Elab` with do-notation (`__elabBind` chains), `pure`, `warnElab`/`failElab`/`failElabWith`/`warnElabWith` (related-origin payloads accepted but rendered as the splice-site origin only), `renderSyntax` (stable fallback rendering per §21.10.1), `syntaxOrigin`/`withSyntaxOrigin`/`normalizeSyntax`/`whnfSyntax` (normalization is identity). The §21.6 convenience queries `defEqSyntax` and `headSymbolSyntax` are provided with `Symbol`/`sameSymbol` declaration-identity comparison (alias paths and static-object rebindings resolve to the same symbol): in an `Elab`-typed position they are ordinary actions, and in any other position the elaborator runs the elaboration-time query at the call site and residualizes its first-order result (`macros/reflection-queries.kp`, `macros/reflection-query-elab.kp`); a payload that elaborates as a type is reflected as the type expression, so `defEqSyntax` relates a same-spelling data family's type facet to its rebound §7.6 static object. Elaboration-time execution runs under the runtime NbE evaluator (`Kappa.Check.runElab` over stuck `__elab*` primitives); spliced syntax is grafted and re-elaborated at the splice site, and expansions feed the §12.2 usage analysis (macro-duplicated linear variables fail at the splice, `macros/duplicate_linear_splice.kp`). Phase checking rejects object-phase runtime locals in splice arguments (`E_ELAB_PHASE`) and bare-`Syntax`-returning prefixed handlers (type-level, §6.3.4.3). Not implemented: declaration splices, quotation-pattern matching (§21.5), the witness-bearing `Core`/`CoreCtx`/`CoreEq` semantic reflection tier of §21.6 (`asCore`/`reifyCore`/`tryProveDefEq`/… — only the `Syntax`-level convenience queries above are provided), `typeOfSyntax`, `currentGoal`/`asCoreGoal`/typed diagnostic values (string codes are accepted), unexpanders/delaborators (§21.10), macro input transcripts, fuel-bounded termination of macro execution (§21.8 relies on the evaluator's fuel only). |
| §22 Derivation-shape reflection | Partial | `std.deriving.shape` as an embedded module: `inspectAdt`/`inspectRecord` (§22.1, with `KAPPA_DERIVING_SHAPE_NOT_DATA`/`NOT_CLOSED_RECORD`/`OPAQUE_REPRESENTATION` for non-data heads, open records, and `opaque data` outside its defining module), shape summaries carrying the names/tags/field-list subset of the §22 records (`sourceName`, `renderName`, `tag`, `fields`; no parameter/quantity/origin/runtime-relevance fields — every field counts as runtime-relevant), `requireRuntimeFieldInstances` (§22.4, probing ordinary instance search; `IsTrait` witnesses synthesized in the §16.3.3 ladder), `matchAdt`/`matchAdt2` generated matching (§22.5; `matchAdt2` lowers to a tupled match with a catch-all duplicating the last different-constructor body), and the §22.7 literal helpers. Record shape field order follows the inspected alias's written order when known (§22.2). Not implemented: `tryInspect*` `Result` forms, `constructAdt`/`constructRecord`/`FieldArgument` (§22.6), `requiredRuntimeFieldConstraints`, `requireRecordFieldInstances`, `matchRecord`. `derive` declarations still parse → `E_UNSUPPORTED`. |
| §23 Staged code | Partial | `Code`/`ClosedCode` (§23.1), quotation `.< e >.` and escape `.~c` (§23.2; the operator runs `.<`, `>.`, and `.~` are reserved staging punctuation — never prefix/infix operators; an escape outside a quote is `E_CODE_ESCAPE_OUTSIDE_QUOTE`), the `Lift`/`liftCode` trait surface (§23.3; the interpreter models generative code by its present-stage value, so a captured simple variable occurrence persists by value without an explicit lift), `closeCode` (§23.4; every constructed `Code` value captures a value environment, hence is closed and scope-safe by construction — closing succeeds), `genlet` (§23.5; sharing is the host's call-by-need thunk sharing, observationally equivalent modulo administrative lets), and `runCode` (§23.6). §23.7 scope safety holds by construction; the §12.3 escape discipline treats a code quote like a closure over its payload, so a quote capturing a borrowed binding that escapes through the result is `E_QTT_BORROW_ESCAPE` (`staging/borrow-escape.kp`). Not implemented: direct `ClosedCode` elaboration of statically closed quotations (§23.4 MAY), `genlet` insertion-point stage errors. |

## Part VI. Dynamic Values, Boundaries, and Interop

| Section | Status | Notes |
| --- | --- | --- |
| §24 Dynamic values | Not implemented | No `Dynamic`, no runtime representations registry. The embedded `std.gradual` module provides the §24.9 canonical type/trait/term surface only (no cast semantics). |
| §25 Boundary contracts, bridges | Not implemented | The embedded `std.bridge` module provides the §25.1 canonical surface only (no boundary semantics). |
| §26 FFI, host bindings | Partial (native) | Native host bindings (§26.1/§27.1.1) are implemented and driven ENTIRELY by the build manifest (no in-compiler catalog). A `nativeBinding` resolves a `host.native.*` raw surface either from an explicit `symbolList` or MECHANICALLY from a real header: `generateFromHeader` (named functions) / `generateAllFromHeader` (every function under a name prefix). Generation runs pkg-config discovery + header preprocessing/parsing (`cc -E`, `Kappa.Backend.HeaderGen`) and the conservative §26.1.4 type mapping (non-`char` pointer ⇒ `Option RawPtr`, `char` pointer ⇒ C-string, exact-width `std.ffi` scalars, array→pointer decay, object-pointer typedef resolution); declarations needing callbacks/function-pointers/by-value structs/variadics/`long long`/`long double` are REJECTED-not-guessed (§26.1.2) — explicit form fails closed, broad form skips+reports. Foreign-call classification metadata (§26.1.4) is carried, pinned in the host-source identity, and enforced against the declared §27.6 capability set (`blocking-cancellable` rejected; `blocking` accepted under advertised `rt-blocking`, realized as single-agent direct execution). Target ABI (§26.1.3): triple pinned + recorded; a cross-capable `zig cc` preprocesses/verifies for the declared triple; a non-LP64 triple is rejected. ABI is verified fail-closed against the real headers (scalar probe) and shim ABI via a force-included prototype header; the surface + every identity input (pkg-config version/.pc digest, header digests, shim digests, symbol surface, triple, classification, capabilities) is pinned in `kappa.lock` and `--locked` verifies it (drift fail-closed). Demonstrated end-to-end by `examples/native/http_uv` (libuv: 210-function broad raw surface + a minimal callback-adapter shim, live HTTP server) and `examples/native/http_sqlite`. Not implemented: managed host bindings (`host.jvm`/`host.dotnet`), bridge-based interop (§26.1.10+), and refined-overlay/trusted-summary strengthening beyond the conservative surface (a user overlay may refine in Kappa, as `runtime.native.kp` does). |
| §27 Backend / runtime capability profiles | Partial (native) | The native backend profile (`zig`/host `cc`, §27.1) is implemented: KCore→C codegen with direct typed call sites (no runtime primitive table / string dispatch), a Boehm-GC runtime (`kappart.c`), `-O2` output, direct typed `host.native` FFI (see §26 row), and the §36 build/package pipeline. §27.6: the profile DECLARES a runtime capability set (`Kappa.Backend.Capabilities`: `rt-core`, `rt-blocking`, `rt-atomics`) recorded in native provenance, and the §26.1.4 classification→capability rule is enforced fail-closed. The tree-walking interpreter remains the engine for `run`/`test`/`audit`. Not implemented: `jvm`/`dotnet`/`wasm` profiles; a concurrent fiber scheduler with a true offloading blocking-work bridge (`rt-blocking` is realized as direct execution on the single agent — sound because no concurrent fiber can starve; tracked in `KAPPA_SELF_HOSTING_PORT_NOTES.md` H-7); the source-level `blocking` combinator (§18.1.11). |

## Part VII. Standard Library Reference

| Section | Status | Notes / tests |
| --- | --- | --- |
| §28.1 Implicit prelude import | Partial | Every file compiles against `std.prelude` (builtins + embedded source, `Kappa.Prelude`), including the fixed unqualified constructor subset (`True`…`refl`). It cannot be disabled (see KNOWN_SPEC_ISSUES.md). |
| §28.2 Normative minimum contents | Partial | Provided: `Void`/`Unit`/`Bool`/`Ordering`/`Option`/`Result`/`List`, aliases `Int`/`Float`/`Char`/`UIO`, `not`/`(&&)`/`(||)` over `Thunk`, the `Show`/`Eq`/`Ord` traits + comparison operators with instances for every §28.2-mandated base type this implementation has — `Eq`: `Unit`, `Bool`, `UnicodeScalar`, `String`, `Integer`, `Nat`, `Double`, `Ordering`; `Ord`: `Bool`, `UnicodeScalar`, `String`, `Integer`, `Nat`, `Double`, `Ordering`; `Show`: `Unit`, `Bool`, `UnicodeScalar`, `String`, `Integer`, `Nat`, `Double`, `Ordering` (aliases `Int`/`Float`/`Char` share them transparently; `traits/base-instances.kp`) — numeric traits per §28.2.1–28.2.3 (`Add`, `Mul`, `Negatable`, `CheckedSub`/`Div`/`Mod` with proof-carrying `(-)`/`(/)`/`(%)`, `FromInteger` for `Nat`/`Integer`/`Double`, `FromFloat` for `Double` incl. literal elaboration), `(++)`, `show`/`print`/`println`/`printString`/`printlnString`, `pure`/`raise`/`throwError`/`catchError`, `identity`, `(\|>)`/`(<\|)`, list functions (`map`, `filter`, `foldl`, `foldr`, `listAppend`, `concatMap`, `listLength`), `orElse`, `newRef`/`readRef`/`writeRef`, `floatEq`. Container/traversal trait stack (the §28.2 "functorial/monadic" and "containers and traversals" minimum): `Functor`/`Foldable`/`Filterable`/`FilterMap`/`Applicative`/`Traversable`/`Monoid`/`Monad`/`Zero`/`One`/`FromString`/`EuclideanSemiring`/`Releasable` with `List`/`Option`/`String`/`Nat`/`Double` instances; member-style `map`/`filter`/`foldr`/`foldl`/`foldMap`/`filterMap`/`traverse`/`(>>=)`; `sequence`, `subtract`/`divide`/`modulo`, `summon`, `(++)` over `Monoid` (`prelude/containers.kp`); the §18.1 concurrency surface with runtime operations — `Duration`/`Instant`/`STM`/`TVar`/`Promise`/`Fiber`/`Scope`/`Monitor`/`FiberRef` types plus `fork`/`await`/`join`/`interrupt`*/`cede`/`timeout`/`race`/`sleepFor`/`atomically`/`newTVar`/`retry`/`newPromise`/`awaitPromise`/`newScope`/`withScope`/`forkIn`/`shutdownScope`/`monitor`/`sandbox`/`newFiberRef` etc. (see the §18.1 and §32 rows); the §18.8.2 completion vocabulary `Exit`/`Cause`/`InterruptCause`/`InterruptTag`/`DefectInfo` and the proof-oriented `Dec` (§28.2); `std.testing.failNow`. Text atoms: `Byte`/`Bytes`/`Grapheme` builtins with `Eq`/`Ord`/`Show` for Byte and Bytes and `Eq`/`Show` (exact scalar sequence, deliberately no `Ord`) for Grapheme (`unicode/quoted-literals.kp`). The §28.2.2 algebraic numeric trait tower (`AdditiveMonoid`/`AdditiveGroup`/`MultiplicativeMonoid`/`Semiring`/`Ring`/`EuclideanSemiring`/`FieldLike`/`OrderedAdditive`/`OrderedSemiring`) is provided as marker traits carrying the spec supertrait graph with the §28.2.3 mandated instances for `Nat`/`Integer`/`Int`/`Rational` and the "MUST NOT" exclusions enforced (proof law members not modeled — KNOWN_SPEC_ISSUES §17); `prelude/algebraic_numeric.kp`, `prelude/algebraic_numeric_exclusions.kp`. `Rational` is a full exact-ratio type with Eq/Ord/Show/Zero/One/Add/Mul/Negatable/CheckedSub/CheckedDiv, FromInteger/FromFloat, and FieldLike (`prelude/rational_run.kp`). The §28.2 collection-basics surface (`array*`/`set*`/`map*`, `listLength`/`listAppend`) is provided over the §20.10.11 as-if list model (`prelude/collection_surface_run.kp`); the proof/equality helpers `absurd`/`subst`/`sym`/`trans`/`cong`/`pathInd`/`unsafeAssertProof`/`witness` resolve at their spec signatures (usable with determined endpoints — KNOWN_SPEC_ISSUES §19; `prelude/proof_helpers_resolve.kp`); `for_`/`sequence_`/`nonZero` and the trait names `Equiv`/`Alternative`/`Iterator`/`MonadError`/`MonadResource`/`MonadRef`/`MonadFinally`/`WellFoundedRelation`/`IsEmpty`/`IsContr`/`IsSubsingleton`/`IsProp`/`IsSet`/`IsGroupoid` are exported. Ranges (§20.2): `Rangeable` with a concrete `NumericRange` carrier, `(..)`/`(..<)`, instances for `Nat`/`Int`/`Integer`/`UnicodeScalar`, and `for`/comprehension iteration via the as-if list lowering plus the `IntoQuery`/`toQuery` surface (`queries/range_iteration_run.kp`). `Eq`/`Ord`/`Show` are now provided for the container types `List`/`Option`/`Result` (premise-driven recursive instances; `Ord (Result e a)` orders `Err` before `Ok`; nested element types resolve through the premise dictionaries — `traits/container-eq-ord-show.kp`). `MonadResource (IO e)` and the `bracket` surface name are provided over the §18.8/§18.1 finally kernel: the release runs exactly once on every exit path from `use` — normal completion, typed failure, and interruption (`effects/bracket.kp`). Missing: `re`/`type` string handlers. |
| §28.2.3 Standard fixities | Implemented | `Kappa.Resolve.defaultFixities`. |
| §29 Required standard modules | Partial | `std.hash` (§29.3): embedded module with opaque `HashCode`/`HashSeed`/`HashState` (HashCode: Eq/Ord only), the linear `HashState` mixing API (`newHashState`/`finishHashState`/`hashUnit`/`hashBool`/`hashUnicodeScalar`/`hashGrapheme`/`hashString`/`hashBytes`/`hashInt`/`hashInteger`/`hashFloatRaw`/`hashDoubleRaw`/`hashNatTag`), `Hashable.hashInto` (borrowed value), `hashField`/`hashWith`, instances for Unit/Bool/Integer/Nat/Double/String/Bytes/Byte/UnicodeScalar/Grapheme/Ordering (FNV-1a mixing; `prelude/std_hash.kp`, `prelude/std_hash_run.kp`); not modeled: the `Eq a =>` superclass on `Hashable`, `hashRational`, and the Option/Result/List/Array container instances. `std.unicode` (§29.4): embedded module over UCD 15.0.0 (`Kappa.UnicodeData`, generated by `tools/gen-unicode-data.py`) — `unicodeVersion`, `utf8Bytes`/`decodeUtf8`/`decodeUtf8Lossy` (U+FFFD policy)/`byteLength`, scalar ops (`scalarValue`/`unicodeScalarFromValue`/`scalarToString`/`scalars`/`scalarCount`), grapheme ops (`graphemeToString`/`graphemeFromString`/`graphemes`/`graphemeCount`, full UAX #29 extended clusters), `normalize`/`isNormalized`/`canonicalEquivalent` (full UAX #15 NFC/NFD/NFKC/NFKD), `caseFold` (full case folding via `Data.Text.toCaseFold`), `displayWidth` (documented coarse one-cell-per-grapheme policy), `words`/`sentences` (documented simple approximations, not UAX #29), the linear `StringBuilder` (`newStringBuilder`/`stringBuilderString`/`Scalar`/`Grapheme`/`finishStringBuilder`), string cursors/spans (`stringStart`/`stringEnd`/`stringCursorOffset`/`stringNextScalar`/`stringPrevScalar`/`stringNextGrapheme`/`stringSpan`/`stringCompact`), and the incremental `Utf8Decoder` (`newUtf8Decoder`/`decodeUtf8Chunk`/`finishUtf8Decoder`) (`unicode/std-unicode-run.kp`, `unicode/scalar-from-value.kp`, `unicode/std-unicode-builders-run.kp`); not provided: the SHOULD collation and security submodules. `std.bytes` (§29.5, MUST): embedded module over the exact byte sequence — `emptyBytes`/`singletonByte`/`bytesLength`/`bytesIsEmpty`/`bytesGet`/`bytesIndex` (proof-carrying)/`bytesAppend`/`bytesConcat`/`bytesSlice` (proof-carrying)/`bytesTake`/`bytesDrop`/`bytesStartsWith`/`bytesEndsWith`/`bytesContains`/`bytesFind`/`bytesBreakOn`/`bytesToList`/`bytesFromList`/`bytesCompact` and the linear `BytesBuilder` (`prelude/std_bytes_run.kp`); not provided: the dependent `SizedArray` views (`bytesAsSizedArray`/`bytesFromSizedArray`). Six further embedded std modules provide the canonical type/trait/term surfaces: `std.ffi`/`std.ffi.c` (§26.1.1), `std.atomic` (§29.1: implemented over `Ref` cells, trivially SeqCst on the cooperative interpreter, with bitwise/representation-equality prims `__intAnd/Or/Xor`, `__atomicRepEq`), `std.gradual` (§24.9), `std.bridge` (§25.1), `std.supervisor` (§29.2); embedded std modules build real import environments and can import each other (`std.bridge` uses `std.gradual.CastBlame`). Not provided: `std.debug`, `std.config`, `std.build`. |

## Part VIII. Core Semantics and Identity

| Section | Status | Notes |
| --- | --- | --- |
| §30.1–30.2 Elaboration, KCore | Partial | Bidirectional elaboration to a pragmatic KCore subset (`Kappa.Core`): de Bruijn terms, Pi with icit+quantity, saturated constructors, match, canonical records/variants, literals, metas. Deliberate delta: do-blocks stay structured (`CDo` + `KItem`) rather than lowering to the spec's completion-kernel combinators; the interpreter executes the kernel semantics directly (§18.8). |
| §31.1 Definitional equality | Partial | NbE conversion: β, δ (conversion-reducible definitions only, §15.1), ι (match/if on known scrutinees), record/variant canonical-form equality, suspension reduction, function η, record η (a literal of projections of one neutral receiver *is* that receiver: `r = (x = r.x, y = r.y)` by refl), seal transparency (`seal e as S = e`; opaque seal projections stick — §13.2.10 row). Irrefutable variables-only tuple/record `let`-patterns destructure by projection rather than a stuck single-case match, so such definitions normalize. Capture-set annotations are part of type identity (canonical `__captures` spines; subsumption is not defeq — §12 row; `equality/defeq-conversions.kp`, `qtt/captures-identity.kp`). ι also consults branch-local rigid facts: a variable match case records the matched nullary constructor or the lacks-set of earlier cases, so residual proofs (e.g. `notLT o = True` in a catch-all arm) discharge by refl (§31.1.2); matches in type position elaborate their arms as types (dependent-match signatures). Quantities participate in Pi identity. Conversion is fuel-bounded; fuel exhaustion is only ever reported as "not equal" (sound, incomplete). |
| §31.2 Erasure | Partial | Erased (quantity-0) implicit constructor parameters are dropped from runtime payloads; full erasure pipeline absent. |
| §31.3 Variant runtime representation | Implemented | Tags are canonical renderings of alias-normalized member types, so `Int` and `Integer` members coincide. |
| §31.4 Record canonicalization | Implemented | Lexicographic canonical field order; initializers still evaluate in source order via a `CLet` chain. |
| §31.5 Elision of mutable state | Not implemented | `var` cells are real `Ref`s at runtime. |
| §32 Runtime semantics | Partial | §32.1 strict, call-by-value, left-to-right evaluation; §18.8 completion kernel with exit actions run exactly once; typed IO failures via catch/finally. Algebraic-effect handlers (§18.1, see the §18.1 row) and the full §18.1.4–18.1.8 concurrency model run in the interpreter on a weak-fair cooperative single-agent scheduler (the threaded RTS pinned to one capability): fibers (fork/forkDaemon/await/join/interrupt-family with structured InterruptCause), `cede`, monotonic timers + interruptible sleep, `timeout`/`race`, STM (TVar/atomically/retry suspend-resume), one-shot promises, explicit supervision scopes + monitors, fiber-local state, and `sandbox`/`unsandbox` (`effects/{scheduler-retry,timeout,race,interrupt,scope,monitor,fiber-local,fiber-fork-await,stm-tvar,promise,timer-duration}.kp`). The native C backend keeps the single-agent direct model (§27 row). |
| §33 Content-addressed identity | Not implemented | |

## Part IX. Compiler, Tooling, Config, and Build

| Section | Status | Notes |
| --- | --- | --- |
| §34 Pipeline, IRs, tooling, lowering | Not implemented | No KFrontIR/KBackendIR, no queries, no dumps. The internal pipeline is parse → resolve → elaborate → interpret (`Kappa.Pipeline`), exposed as `kappa (check\|run\|test)`. |
| §35 Config mode | Not implemented | |
| §36 Build system | Not implemented | |

## Part X. Tooling, IDE, and Interactive Semantic Services

| Section | Status | Notes |
| --- | --- | --- |
| §37 LSP/IDE services | Not implemented | |

## Appendix T. Test harness

| Section | Status | Notes |
| --- | --- | --- |
| §T.2–T.8 | Partial | Single-file inline tests, directory suites (`suite.ktest`/`main.kp`/direct directory argument, multi-file compilation in import order), the §T.3 directive subset listed in TESTING.md, inline `--!!` markers, unsupported-classification per §T.8. `mode compile` runs the full pipeline without executing an entry; the per-module Term→Value lowering is recorded as the `lowerKBackendIR`/`module` portable trace step (§T.5.5). `scriptMode` (§T.4) compiles without §8.1 path-derived module names. Supported `x-` extension directives per §T.3/§T.1: `x-assertEval`, `x-assertEvalErrorContains`, `x-assertDeclDescriptors`, `x-assertTraitMembers`, `x-assertContainsTokenKinds`/`x-assertContainsTokenTexts`, `x-assertModule`, `x-assertModuleAttributes`, `x-assertDataConstructors` (subjects resolve in the directive file's own module per §T.6; values render precedence-aware). `mode analyze`, non-interpreter backends, unprovided capabilities, stdin/args, dump assertions: classified unsupported. |
